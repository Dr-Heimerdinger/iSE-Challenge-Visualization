Generate a complete Python script for a Gradio interface based on the following machine learning task.

Here is the detailed information parsed from the `task.yaml` file:

**1. Task Description:**
- **Type:** {task_type}
- **Description:** {task_description}
- **User Input Description:** {input_description}
- **Desired Output Description:** {output_description}

**2. Visualization Requirements:**
- **Overall Description:** {visualize_description}
- **Interface Features:** {visualize_features}
- **Example Data Path:** {data_path}

**3. Model Information:**
- **API URL:** {api_url}
- **Model Name:** {model_name}
- **Input Format:** {model_input_format}
- **Output Format:** {model_output_format}
- **Additional Files (if any):** {other_data}

**Requirements for the generated script:**
- It must be a single, executable Python script.
- It must use the Gradio library to create the user interface.
- It must correctly call the model's API at `{api_url}`.
- The interface must provide a way for users to input new data. 
- The interface must display the model's predictions in a clear and user-friendly format. 
- The script must include and load examples from the `{data_path}` directory using `gradio.Examples`.